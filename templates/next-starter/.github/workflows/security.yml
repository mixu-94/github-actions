name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  security:
    uses: mixu-94/github-actions/.github/workflows/security.yml@d2326f4fd069304b4e74968471b83ca2c9142ae7 # v1 baseline
    with:
      blocking: false
