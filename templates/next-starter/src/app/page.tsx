export default function Home() {
  return (
    <main>
      <section>
        <p className="eyebrow">mixu-94 / next-starter</p>
        <h1>Start with the product, not the boilerplate.</h1>
        <p>
          TypeScript, linting, CI, security checks, and conservative dependency
          maintenance are already part of the repository setup.
        </p>
      </section>
    </main>
  );
}
