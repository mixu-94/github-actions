name: Repository checks

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  security:
    name: Security
    permissions:
      contents: read
    uses: ./.github/workflows/security.yml
    with:
      dependency-review: false
      blocking: true
